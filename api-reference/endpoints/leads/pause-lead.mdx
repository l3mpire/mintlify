---
openapi: post /leads/pause/{leadId}
description: Pauses a lead's activity in all campaigns or a specific campaign.
---
import {SnippetObjectReference} from '/snippets/snippet-object-reference.jsx'

<SnippetObjectReference objectName="Lead" objectPath="lead" />

Use the `campaignId` query parameter to pause the lead in a specific campaign only.