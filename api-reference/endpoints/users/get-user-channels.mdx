---
openapi: get /user/channels
description: Retrieves the connected channels (email, LinkedIn, WhatsApp) and their availability for the authenticated user.
---

<Note>
This endpoint uses the API key to identify the user. No additional parameters are required.
</Note>
