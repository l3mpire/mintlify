---
title: Create Task
openapi: post /tasks
---
import {SnippetObjectReference} from '/snippets/snippet-object-reference.jsx'

<SnippetObjectReference objectName="Task" objectPath="task" />

Create a manual task (opportunity) associated with a lead. The task must be assigned to a team member and have a due date. Optional fields let you set a title, message, and priority.