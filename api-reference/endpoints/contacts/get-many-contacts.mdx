---
openapi: get /contacts
description: Retrieves contacts from your CRM. Use `idsOrEmails` to fetch specific contacts by ID or email in a single request (max 100), `crmId` / `crmIds` to look them up by their record id in your connected CRM, or omit all of these to search/list contacts by name, email, contact list, or campaign membership.
---
import {SnippetObjectReference} from '/snippets/snippet-object-reference.jsx'

<SnippetObjectReference objectName="Contact" objectPath="contact" />

## Query modes

This endpoint supports several query modes:

| Mode | Parameters | Response format |
|------|-----------|----------------|
| **By IDs/emails** | `idsOrEmails` | Array of contacts |
| **By CRM ID** | `crmId` | Single contact object (`404` if none) |
| **By CRM IDs** | `crmIds` | Array of contacts (found subset, `[]` if none) |
| **Search/filter** | `search`, `email`, `listId`, and/or `notInAnyCampaign` | Paginated object with `data`, `total`, `limit`, `offset` |

The `crmId` / `crmIds` lookups resolve against the team's active CRM (HubSpot / Salesforce / Pipedrive), matching on the contact's CRM record id.

### Filtering by contact list

Use the `listId` parameter to retrieve contacts belonging to a specific list. Get valid list IDs from `GET /contacts/lists`.

You can combine `listId` with `search` or `email` to further narrow results within a list.

### Filtering contacts not in any campaign

Use `notInAnyCampaign=true` to find contacts that are not part of any campaign (orphan contacts). This can be used alone or combined with other filters like `search`, `email`, or `listId`.